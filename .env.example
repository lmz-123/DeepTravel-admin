# Existing Travel MySQL connection. When both projects share a Docker network,
# replace the host with that MySQL service name.
DATABASE_URL=mysql+pymysql://jiandi:jiandi_dev@172.17.0.1:3307/jiandi?charset=utf8mb4
ADMIN_TOKEN=DeepTravelAdmin2026
MEDIA_HOST_PATH=/root/DeepTravel/backend/media
CORS_ORIGINS=*
MAX_UPLOAD_MB=30
